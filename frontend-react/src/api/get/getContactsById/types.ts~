import { IContact, IResponseData } from '@/api/types';

export interface IGetAllContactsParams {
  id: number;
}

export interface IGetContactsByIdResponse extends IResponseData {
  data: IContact;
}
